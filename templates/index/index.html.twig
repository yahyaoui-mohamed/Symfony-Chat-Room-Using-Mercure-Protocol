{% extends 'base.html.twig' %}

{% block title %}Chat App{% endblock %}

{% block body %}
    <div class="login">
        {{ form_start(form) }}
            <div class="d-flex">
                <div class="form-group">
                    {{ form_widget(form.name) }}
                </div>
                <div class="form-group">
                    {{ form_row(form.go) }}
                </div>
            </div>

        {{ form_end(form) }}
    </div>

{% endblock %}
